package com.market.monitor.enums;

public enum ConditionType {
    /** Trigger when current price is strictly above the threshold */
    ABOVE,
    /** Trigger when current price is strictly below the threshold */
    BELOW
}